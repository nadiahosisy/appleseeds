// Which of the following is/are strings?

// c) '4' - String.

// Which of the following is/are numbers?
// Numbers:
// a) 4
// b) 4.0
// d) -4

// The booleans are:
// a) true
// b) false

// Explanation: In JavaScript, booleans represent the logical values true and false, which are used for logical operations and conditional statements. The values "true" in option c) is a string, not a boolean.

// The result of 80 + 71.2 is:
// a) 151.2

// Explanation: When you add a number to a floating-point number, the result is a floating-point number.

// The result of "80" + 71.2 is:
// c) "8071.2"

// Explanation: When you concatenate a string with any other value, JavaScript converts the other value to a string and performs string concatenation.

// The expression 100 + 30 produces:
// a) number

// Explanation: When you add two numbers, the result is a number.

// The expression "100" + 30 produces:
// b) string

// Explanation: When you concatenate a string with any other value, JavaScript converts the other value to a string and performs string concatenation, resulting in a string.



// Three different ways to declare variables
let myVariable = 10;
var anotherVariable = 'Hello';
const thirdVariable = true;

// Declare a variable and reassign a value to it
let myNumber = 5;
myNumber = 10;

// Create a variable and assign a string
let myString;
myString = "He's got it!";

// 1. Create a variable and assign a value on how much a restaurant bill is.
let restaurantBill = 50.25;

// 2. Create a variable and assign a value on how much tax they should pay.
let taxAmount = 0.1;

// 3. Create a variable that will calculate the bill * tax
let totalBill = restaurantBill * (1 + taxAmount);

// Output the result
console.log(`Your total bill is ${totalBill} $.`);

// Round the number 50.6 to its nearest integer
let roundedNumber = Math.round(50.6);

// Create a variable that is undefined
let undefinedVariable;
